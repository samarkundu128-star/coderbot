name: Automatic Render Deploy

on:
  push:
    branches:
      - main  # Jab bhi main branch par code update hoga tabhi chalega

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deployment
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
